The persistent top strip on every screen — active room (accent dot), group count, connectivity icon, and clock.

```jsx
<StatusBar room="Kitchen" grouped={1} time="14:32" source="wifi" />
```

`source` is any Lucide icon name (wifi / bluetooth / cast). Requires Lucide on the page.
