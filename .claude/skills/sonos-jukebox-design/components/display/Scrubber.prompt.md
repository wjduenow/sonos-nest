Progress bar for the Now Playing view. Shows an accent fill, a draggable-looking handle, and mono timecodes.

```jsx
<Scrubber elapsed={84} duration={251} />
<Scrubber live />
```

Pass `live` for radio — the bar fills, the handle disappears, and it reads LIVE · on air.
