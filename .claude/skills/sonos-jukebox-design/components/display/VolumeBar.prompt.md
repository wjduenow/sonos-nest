Inline volume level used in room rows and the group panel. The speaker icon reflects level (mute / low / high).

```jsx
<VolumeBar value={64} />
<VolumeBar muted />
```

For the full-screen turn-the-knob overlay use `Dial` instead. Requires Lucide.
