On-screen dial that mirrors the physical rotary control — a 270° accent arc with a big center value. Use for the volume overlay that appears when the user turns the knob.

```jsx
<Dial value={62} label="VOLUME" size={180} />
```

Purely presentational; drive `value` from the encoder state.
