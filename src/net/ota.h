// OTA firmware updates (stable / nightly channels). Stretch feature — plan §4/§7 Phase 4.
#pragma once

void otaBegin();          // version check + update; no-op stub for now
// TODO Phase 4: version check, retry loop, progress UI, stable/nightly channels.
